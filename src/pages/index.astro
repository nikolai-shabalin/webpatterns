---
import Layout from '../layouts/Layout.astro';
import Pagination from '../components/Pagination.astro';
import Breadcrumbs from '../components/Breadcrumbs.astro';
---

<Layout>
	<main class="container">
		<header class="hero">
			<div class="hero-content">
				<div class="hero-badge">🚀 Веб-паттерны</div>
				<h1>Создавайте отличные интерфейсы</h1>
				<p class="subtitle">Коллекция проверенных решений для современной веб-разработки</p>

			</div>

		</header>

		<section class="description">
			<div class="section-header">
				<h2>Что такое веб-паттерны?</h2>
				<p class="section-subtitle">Готовые решения для типичных задач веб-разработки</p>
			</div>
			<div class="features-grid">
				<div class="feature">
					<div class="feature-icon">🔧</div>
					<h3>Готовые решения</h3>
					<p>Проверенные временем подходы к созданию пользовательских интерфейсов</p>
				</div>
				<div class="feature">
					<div class="feature-icon">⚡</div>
					<h3>Быстрая разработка</h3>
					<p>Ускоряем процесс создания интуитивно понятных веб-приложений</p>
				</div>
				<div class="feature">
					<div class="feature-icon">🎯</div>
					<h3>Консистентность</h3>
					<p>Обеспечиваем единообразие интерфейса во всем приложении</p>
				</div>
			</div>
		</section>

		<section class="patterns">
			<div class="section-header">
				<h2>Доступные паттерны</h2>
				<p class="section-subtitle">Выберите нужный компонент для вашего проекта</p>
			</div>
			<div class="patterns-grid">
				<div class="pattern-card">
					<div class="card-header">
						<div class="pattern-icon">📄</div>
						<h3>Пагинация</h3>
					</div>
					<p>Навигация по страницам контента с удобным переключением между ними.</p>
					<div class="pattern-demo">
						<Pagination currentPage={3} totalPages={10} />
					</div>
					<a href="/patterns/pagination" class="pattern-link">
						Подробнее
						<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
							<path d="M5 12h14M12 5l7 7-7 7"/>
						</svg>
					</a>
				</div>

				<div class="pattern-card">
					<div class="card-header">
						<div class="pattern-icon">🍞</div>
						<h3>Хлебные крошки</h3>
					</div>
					<p>Навигационная цепочка, показывающая путь к текущей странице.</p>
					<div class="pattern-demo">
						<Breadcrumbs
							items={[
								{ name: 'Главная', url: '/' },
								{ name: 'Паттерны', url: '/patterns' },
								{ name: 'Хлебные крошки', url: '/patterns/breadcrumbs' }
							]} 
						/>
					</div>
					<a href="/patterns/breadcrumbs" class="pattern-link">
						Подробнее
						<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
							<path d="M5 12h14M12 5l7 7-7 7"/>
						</svg>
					</a>
				</div>
			</div>
		</section>
	</main>
</Layout>

<style>
	.container {
		margin-inline: max(20px, (100% - 1200px) / 2);
		font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
		line-height: 1.6;
		color: #1a1a1a;
	}

	/* Hero Section */
	.hero {
		position: relative;
		text-align: center;
		padding: 80px 0 100px 0;
		background: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #f093fb 100%);
		color: white;
		margin: -20px -20px 60px -20px;
		border-radius: 0 0 30px 30px;
		overflow: hidden;
	}

	.hero::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23ffffff' fill-opacity='0.05'%3E%3Ccircle cx='30' cy='30' r='2'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
		opacity: 0.3;
	}

	.hero-content {
		position: relative;
		z-index: 2;
	}

	.hero-badge {
		display: inline-block;
		background: rgba(255, 255, 255, 0.2);
		backdrop-filter: blur(10px);
		padding: 8px 16px;
		border-radius: 20px;
		font-size: 0.9rem;
		font-weight: 600;
		margin-bottom: 20px;
		border: 1px solid rgba(255, 255, 255, 0.3);
	}

	.hero h1 {
		font-size: clamp(2.5rem, 5vw, 4rem);
		margin: 0 0 20px 0;
		font-weight: 800;
		letter-spacing: -0.02em;
		line-height: 1.1;
	}

	.subtitle {
		font-size: clamp(1.1rem, 2vw, 1.3rem);
		opacity: 0.9;
		margin: 0 0 40px 0;
		font-weight: 400;
		max-width: 600px;
		margin-left: auto;
		margin-right: auto;
	}



	/* Section Headers */
	.section-header {
		text-align: center;
		margin-bottom: 60px;
	}

	.section-header h2 {
		font-size: clamp(2rem, 4vw, 2.5rem);
		margin-bottom: 16px;
		color: #1a1a1a;
		font-weight: 700;
		letter-spacing: -0.02em;
	}

	.section-subtitle {
		font-size: 1.1rem;
		color: #666;
		margin: 0;
		max-width: 500px;
		margin-left: auto;
		margin-right: auto;
	}

	/* Features Grid */
	.features-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
		gap: 30px;
		margin-bottom: 80px;
	}

	.feature {
		text-align: center;
		padding: 40px 20px;
		background: white;
		border-radius: 16px;
		box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
		transition: all 0.3s ease;
		border: 1px solid rgba(0, 0, 0, 0.05);
	}

	.feature:hover {
		transform: translateY(-8px);
		box-shadow: 0 12px 40px rgba(0, 0, 0, 0.12);
	}

	.feature-icon {
		font-size: 3rem;
		margin-bottom: 20px;
		display: block;
	}

	.feature h3 {
		font-size: 1.3rem;
		margin-bottom: 12px;
		color: #1a1a1a;
		font-weight: 600;
	}

	.feature p {
		color: #666;
		margin: 0;
		line-height: 1.6;
	}

	/* Patterns Section */
	.patterns {
		margin-bottom: 80px;
	}

	.patterns-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
		gap: 30px;
	}

	.pattern-card {
		background: white;
		border-radius: 20px;
		padding: 32px;
		box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
		transition: all 0.3s ease;
		border: 1px solid rgba(0, 0, 0, 0.05);
		position: relative;
		overflow: hidden;
	}

	.pattern-card::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 4px;
		background: linear-gradient(90deg, #667eea, #764ba2);
		transform: scaleX(0);
		transition: transform 0.3s ease;
	}

	.pattern-card:hover {
		transform: translateY(-8px);
		box-shadow: 0 12px 40px rgba(0, 0, 0, 0.12);
	}

	.pattern-card:hover::before {
		transform: scaleX(1);
	}

	.card-header {
		display: flex;
		align-items: center;
		gap: 12px;
		margin-bottom: 16px;
	}

	.pattern-icon {
		font-size: 1.5rem;
		width: 40px;
		height: 40px;
		display: flex;
		align-items: center;
		justify-content: center;
		background: linear-gradient(135deg, #667eea, #764ba2);
		color: white;
		border-radius: 10px;
	}

	.pattern-card h3 {
		font-size: 1.4rem;
		margin: 0;
		color: #1a1a1a;
		font-weight: 600;
	}

	.pattern-card p {
		color: #666;
		margin-bottom: 24px;
		line-height: 1.6;
	}

	.pattern-demo {
		background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
		padding: 24px;
		border-radius: 12px;
		margin-bottom: 24px;
		display: flex;
		justify-content: center;
		align-items: center;
		min-height: 80px;
		border: 1px solid rgba(0, 0, 0, 0.05);
	}



	.pattern-link {
		display: inline-flex;
		align-items: center;
		gap: 8px;
		color: #667eea;
		text-decoration: none;
		font-weight: 600;
		transition: all 0.3s ease;
		padding: 8px 0;
	}

	.pattern-link:hover {
		color: #764ba2;
		gap: 12px;
	}

	.pattern-link svg {
		transition: transform 0.3s ease;
	}

	.pattern-link:hover svg {
		transform: translateX(4px);
	}

	@media (max-width: 768px) {
		.hero {
			padding: 60px 0 80px 0;
		}

		.patterns-grid {
			grid-template-columns: 1fr;
		}

		.pattern-card {
			padding: 24px;
		}

		.features-grid {
			grid-template-columns: 1fr;
		}

		.feature {
			padding: 30px 20px;
		}
	}

	@media (max-width: 480px) {
		.container {
			padding: 0 16px;
		}
	}
</style>
